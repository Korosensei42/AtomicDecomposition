load_package redlog;
rlset OFSF;
off rlverbose;
on rlnzden;

vars := {m0, c0, m1};

    formula := ((0 = (m0 + m1
)
) and ((0 <= m0
) and ((c0 = 0
) impl (m0 = 0
)
)
)
);
query := (rlqea ex(vars, formula));

end;